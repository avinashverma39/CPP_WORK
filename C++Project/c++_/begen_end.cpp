#include <iostream>
#include <string>
#include <algorithm>
using namespace std;

int main()
{
    // string str;
    // cout << "Enter a string : ";
    // cin >> str;
    // reverse(str.begin(), str.end());
    // cout << "Reverse The String " << str;
    // return 0;

    string str1 = "";
    if (str1.empty())
    {
        cout << "String is empty";
    }
    else
    {
        cout << "String is not empty";
    }
}